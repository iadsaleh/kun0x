# 🚀 دليل رفع الموقع الستاتيكي على GitHub و Netlify

## 📋 نظرة عامة

هذا الدليل يشرح خطوة بخطوة كيفية:
1. رفع الموقع الستاتيكي على GitHub
2. ربطه مع Netlify للنشر المباشر
3. الحصول على رابط مباشر للموقع

---

## 📝 المتطلبات قبل البدء

### 1. حساب GitHub
- إذا لم يكن لديك حساب، سجل على: https://github.com/signup
- سجّل الدخول إلى حسابك

### 2. حساب Netlify
- سجّل على: https://www.netlify.com/
- يمكنك التسجيل باستخدام حساب GitHub (موصى به)

### 3. Git مثبت على جهازك
- تحقق من التثبيت:
  ```powershell
  git --version
  ```
- إذا لم يكن مثبتاً، حمّله من: https://git-scm.com/downloads

---

## 🔧 خطوة 1: تحضير المشروع

### 1.1 افتح PowerShell في مجلد الموقع

```powershell
cd "c:\Users\iadSa\Downloads\Telegram Desktop\2\static_site"
```

### 1.2 تحديث رابط البوت (مهم!)

افتح ملف `index.html` وابحث عن السطر رقم 135:

```html
<a href="https://t.me/YourBotUser" target="_blank"
```

**غيّر `YourBotUser` إلى اسم بوتك الحقيقي!**

مثال:
```html
<a href="https://t.me/GameCatalogBot" target="_blank"
```

---

## 📦 خطوة 2: إنشاء مستودع Git محلي

### 2.1 تهيئة Git

```powershell
git init
```

**النتيجة المتوقعة:**
```
Initialized empty Git repository in c:/Users/iadSa/Downloads/Telegram Desktop/2/static_site/.git/
```

### 2.2 إضافة الملفات

```powershell
git add .
```

### 2.3 التحقق من الملفات المضافة

```powershell
git status
```

**تأكد من:**
- ✅ `index.html` مضاف
- ✅ `data.json` مضاف
- ✅ `js/app.js` مضاف
- ✅ `README.md` مضاف
- ✅ `netlify.toml` مضاف
- ❌ `kun_nexus.db` **غير** مضاف (محمي بواسطة .gitignore)

### 2.4 إنشاء أول Commit

```powershell
git config --global user.name "اسمك"
git config --global user.email "بريدك@example.com"

git commit -m "Initial commit: Nexus Game Catalog"
```

---

## 🌐 خطوة 3: إنشاء مستودع على GitHub

### 3.1 افتح GitHub
1. اذهب إلى: https://github.com/
2. سجّل الدخول
3. اضغط على زر **"+"** في الأعلى
4. اختر **"New repository"**

### 3.2 إعدادات المستودع

املأ المعلومات التالية:

| الحقل | القيمة الموصى بها |
|-------|-------------------|
| **Repository name** | `nexus-game-catalog` |
| **Description** | `A modern game catalog showcasing available games` |
| **Visibility** | ✅ **Public** (ضروري لـ Netlify المجاني) |
| **Initialize** | ❌ لا تختر أي شيء (لديك ملفات بالفعل) |

### 3.3 إنشاء المستودع

اضغط على **"Create repository"**

---

## 📤 خطوة 4: رفع الملفات إلى GitHub

بعد إنشاء المستودع، ستظهر لك صفحة بها أوامر. استخدم الأوامر التالية:

### 4.1 ربط المستودع المحلي مع GitHub

```powershell
# استبدل YOUR_USERNAME باسم المستخدم على GitHub
git remote add origin https://github.com/YOUR_USERNAME/nexus-game-catalog.git
```

**مثال:**
```powershell
git remote add origin https://github.com/ahmad123/nexus-game-catalog.git
```

### 4.2 تغيير اسم الفرع إلى main

```powershell
git branch -M main
```

### 4.3 رفع الملفات

```powershell
git push -u origin main
```

**ستُطلب منك:**
- اسم المستخدم على GitHub
- كلمة المرور (أو Personal Access Token)

> [!TIP]
> **إذا طلب منك Token بدلاً من Password:**
> 1. اذهب إلى: https://github.com/settings/tokens
> 2. اضغط **"Generate new token (classic)"**
> 3. اختر صلاحية **repo**
> 4. استخدم الـ Token كـ password

### 4.4 التحقق

افتح رابط المستودع على GitHub:
```
https://github.com/YOUR_USERNAME/nexus-game-catalog
```

يجب أن ترى جميع ملفاتك! ✅

---

## 🎉 خطوة 5: نشر الموقع على Netlify

### 5.1 تسجيل الدخول إلى Netlify

1. اذهب إلى: https://app.netlify.com/
2. سجّل الدخول (استخدم GitHub للتسهيل)

### 5.2 إضافة موقع جديد

1. اضغط **"Add new site"** → **"Import an existing project"**
2. اختر **"Deploy with GitHub"**
3. اختر مستودع `nexus-game-catalog`

### 5.3 إعدادات النشر

| الإعداد | القيمة |
|---------|--------|
| **Branch to deploy** | `main` |
| **Build command** | (اتركه فارغاً) |
| **Publish directory** | `.` (نقطة) |

اضغط **"Deploy site"**

### 5.4 انتظر النشر

سيستغرق 1-2 دقيقة. شاهد سجل النشر (Deploy log).

### 5.5 احصل على رابط الموقع

بعد انتهاء النشر، ستحصل على رابط مثل:
```
https://random-name-123456.netlify.app
```

**🎊 تهانينا! موقعك الآن مباشر على الإنترنت!**

---

## ⚙️ خطوة 6: تخصيص الإعدادات (اختياري)

### 6.1 تغيير اسم الموقع

1. في Netlify Dashboard، اختر موقعك
2. اذهب إلى **"Site settings"** → **"General"**
3. في **"Site details"**، اضغط **"Change site name"**
4. اختر اسم مميز مثل: `nexus-games` أو `game-catalog-pro`
5. الرابط الجديد سيكون: `https://your-name.netlify.app`

### 6.2 ربط دومين خاص (اختياري)

إذا كان لديك دومين خاص:

1. اذهب إلى **"Domain settings"**
2. اضغط **"Add custom domain"**
3. أدخل الدومين (مثل: `games.example.com`)
4. اتبع التعليمات لتحديث DNS

---

## 🔄 خطوة 7: تحديث الموقع في المستقبل

### 7.1 تحديث البيانات محلياً

عندما تريد تحديث الألعاب:

```powershell
# 1. حدّث قاعدة البيانات
# 2. شغّل المزامنة
cd "c:\Users\iadSa\Downloads\Telegram Desktop\2"
python manage_site.py
# اضغط Publish في واجهة المتصفح

# 3. اختياري: جلب الصور
python auto_fetch_assets.py
```

### 7.2 رفع التحديثات إلى GitHub

```powershell
cd "c:\Users\iadSa\Downloads\Telegram Desktop\2\static_site"

git add data.json
git commit -m "Update game catalog"
git push
```

### 7.3 النشر التلقائي

Netlify سيكتشف التغييرات تلقائياً في GitHub وينشر آخر إصدار خلال دقائق! 🚀

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: Git غير معرّف

```
git: The term 'git' is not recognized
```

**الحل:**
- ثبّت Git من: https://git-scm.com/downloads
- أعد تشغيل PowerShell بعد التثبيت

### المشكلة 2: خطأ في Push إلى GitHub

```
Permission denied (publickey)
```

**الحل:**
- استخدم HTTPS بدلاً من SSH
- أو أنشئ Personal Access Token: https://github.com/settings/tokens

### المشكلة 3: الموقع لا يظهر على Netlify

**الحل:**
1. تحقق من سجل النشر (Deploy log) في Netlify
2. تأكد من أن **Publish directory** هو `.` (نقطة)
3. تأكد من أن ملف `index.html` موجود في جذر المستودع

### المشكلة 4: الصور لا تظهر

**الحل:**
- تأكد من أن روابط الصور في `data.json` صحيحة
- تحقق من أن `icon_url` و `header_image_url` تبدأ بـ `https://`
- استخدم `auto_fetch_assets.py` لجلب الصور تلقائياً

### المشكلة 5: ملف kun_nexus.db ظهر في GitHub

**الحل:**
```powershell
# احذف الملف من Git (بدون حذفه محلياً)
git rm --cached kun_nexus.db

# حدّث الـ commit
git commit -m "Remove database file"
git push
```

---

## 📊 معلومات إضافية

### حجم المشروع

| الملف | الحجم التقريبي |
|-------|----------------|
| `index.html` | ~34 KB |
| `data.json` | ~280 KB |
| `js/app.js` | (تحقق محلياً) |
| **المجموع** | أقل من 1 MB |

### الملفات المستبعدة تلقائياً

بفضل `.gitignore`:
- ❌ `kun_nexus.db` (19 MB) - قاعدة بيانات حساسة
- ❌ ملفات `.txt` المحلية
- ❌ ملفات النظام

---

## 🎯 الخطوات التالية

بعد النشر الناجح:

1. ✅ شارك الرابط مع المستخدمين
2. ✅ أضف الرابط في وصف بوتك على Telegram
3. ✅ راقب الأداء على Netlify Analytics
4. ✅ حدّث البيانات بانتظام

---

## 📞 دعم

إذا واجهت مشاكل:

- **GitHub Help:** https://docs.github.com/
- **Netlify Docs:** https://docs.netlify.com/
- **Git Tutorial:** https://git-scm.com/book/en/v2

---

## ✨ نصائح للنجاح

> [!TIP]
> **نصائح مهمة:**
> 1. احتفظ بنسخة احتياطية محلية من `data.json`
> 2. اختبر الموقع محلياً قبل الرفع
> 3. استخدم أسماء commit واضحة
> 4. حدّث الموقع بانتظام لجذب الزوار

---

**آخر تحديث:** 17 فبراير 2026  
**الإصدار:** 1.0

🎉 **بالتوفيق في نشر موقعك!**
